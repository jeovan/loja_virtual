<%= form_with(model: dashboard_produto) do |form| %>
  <% if dashboard_produto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dashboard_produto.errors.count, "error") %> prohibited this dashboard_produto from being saved:</h2>

      <ul>
        <% dashboard_produto.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao %>
  </div>

  <div class="field">
    <%= form.label :preco %>
    <%= form.text_field :preco %>
  </div>

  <div class="field">
    <%= form.label :quantidade %>
    <%= form.number_field :quantidade %>
  </div>

  <div class="field">
    <%= form.label :categoria_id %>
    <%= form.collection_select(:categoria_id , Dashboard::Categoria.all,:id,:nome) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
